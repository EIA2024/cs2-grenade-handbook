# 04 · 本次验收记录

版本：1.1.0，Windows x64 自包含。构建与验证日期：2026-09-21。

## 已完成

- 新版独立目录发布成功：artifacts/道具手册-v1.1.0。旧版运行时文件被占用，因此未强制关闭旧版，改用新目录交付。
- 对新版发布目录 EXE 执行验证：**20 项通过，0 项失败**。
- 输入：键盘／五键鼠标绑定、重复绑定及纯修饰键拒绝、旧键盘绑定迁移、按住去重、释放后再按、修饰组合和切换后的清理。
- 导航：多层进入与返回、首尾循环、草稿／空分类过滤、同目录道具与子目录并存、确认／取消和恢复、空资料状态。
- 窗口：后台菜单与图片查看均保留穿透、不激活样式，打开菜单不扩大窗口；本项目前台窗口在后台菜单与隐藏／显示操作中保持活动状态。
- 生命周期：在真实 Windows API 上完成 Raw Input 注册和注销，验证隐藏仍保留注册、返回编辑器注销。
- 设置：使用真实文件锁制造保存失败，验证旧绑定和文件内容不变；之后整组保存成功。
- 回归：中文说明、PNG/JPEG、ZIP 往返与原包保留、目录编辑和回收恢复、草稿与损坏图片、非法 ZIP 路径／链接／加密／CRC／超限拒绝。
- 检查自身控件渲染：双图查看、后台菜单和绑定设置。截图并非屏幕录制。
- 提供单独的前台“真实按键测试”进程；只记录自身收到的绑定次数和鼠标事件，不保存日志、不模拟输入。

详细日志：测试结果/20260921-155459/验证结果.txt。
同目录的“发布验证.json”记录通过验证的 EXE、Desktop DLL 和 Core DLL 的 SHA-256，打包时要求与当前发布文件一致。便携包附验证日志、验证凭据和界面渲染。

## 未验证的实际行为

尚未由用户进行真实键鼠双响应和下层窗口点击穿透验证。自动调用导航命令、检查样式及自身窗口焦点不能替代这项操作。

未启动、读取或控制 CS2；游戏内准心持续控制、同键响应、无边框／独占全屏可见性、实际 DPI／多显示器和游戏帧率仍待用户手动验证。操作步骤见“01-使用说明”。

没有 Valve/VAC 或其他平台的认证结论。技术边界、自动测试、真实交互与游戏兼容分别记录。
