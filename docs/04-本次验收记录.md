# 04 · 本次验收记录

版本：1.1.1，Windows x64 自包含。完成日期：2026-09-23。

## 已通过

- Release 构建与自包含发布成功。源码已分为 Core、Storage、Desktop；桌面内部按职责分目录。
- 对实际发布 EXE 验证：**29 项通过，0 项失败**。
- 原有编辑、导航、输入绑定、中文资料、ZIP 往返、原包保留、回收恢复与保存失败保护回归通过。
- 新增损坏/null 设置恢复、旧设置原文备份、非法资料位置恢复、有限元数据读取和坏回收记录隔离。
- 拒绝 ZIP 可执行附件、非法 UTF-8、UNC/设备/ADS 路径及 Windows 特殊保留名。
- 锁定旧图片后尝试换格式，失败不提交新图，原图保持；解锁后替换成功。
- 10001目录和65份1MB说明测试确认扫描预算生效，返回已有结果和提示。
- 本软件窗口的穿透/不激活样式与前台测试窗口活动状态检查通过；已复查后台菜单、编辑器的自身控件渲染。
- 独立发布脚本检查通过：不收集清单外文件、拒绝二进制变化、拒绝清单路径越界。
- 静态边界检查通过：原生声明仅在Windows模块；无列出的禁止进程/输入/网络调用；asInvoker和uiAccess=false声明保持。

详细日志：测试结果/20260923-020629/验证结果.txt。
发布凭据：同目录“发布验证.json”，记录4个应用二进制和整份发布清单的SHA-256。打包核对清单中所有运行时文件，附带日志与界面渲染。

安全问题和修复逐项见“05-安全审查与修复”；架构及后续开发入口见“06-开发者架构”。历史1.0和1.1记录另存，不替代本次结果。

## 未验证及结论边界

没有启动、读取或控制CS2，也没有运行输入模拟。真实键鼠双响应、下方程序实际点击穿透、游戏可见性、DPI/多显示器和游戏帧率仍需用户手动检查。

Raw Input注册/注销失败、样式读取失败等原生故障路径已审代码，没有使用驱动或故障注入制造OS故障。窗口设置保存失败不阻止退出的流程已审查，未在GUI中自动操作错误对话框。

静态扫描和功能通过不等于操作系统沙箱、代码签名或VAC官方认证。包内没有用户资料库或用户设置；更新前从托盘退出旧版，保留/备份自己的资料目录。
