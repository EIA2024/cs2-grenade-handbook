# 04 · 本次验收记录

版本：1.0.0，Windows x64 自包含；构建日期：2026-09-21。

## 已完成

- .NET 10 Release 构建和自包含发布成功。
- 对交付目录中的 EXE 执行自动验收：**13 项通过，0 项失败**。
- 用占位双图验证了创建、编辑、ZIP 往返、原包保留、草稿、目录操作与回收恢复。
- 验证超长说明及真实文件锁导致的保存失败不会覆盖原说明。
- 验证非法路径、重复路径、加密标志、CRC 错误、超限解压、ZIP 符号链接与损坏图片的拒绝行为。
- 创建本软件 WPF 窗口并切换选择／查看、隐藏／显示、普通／置顶，检查了穿透、分层及不激活样式。
- 已检查默认 520×380 双图面板和编辑器渲染；其他逻辑尺寸渲染也保留在项目测试目录。
- 代码中的五种直接 user32 API 仅接收本软件窗口。另有用户主动打开资料目录的 Shell 操作。

完整日志来自 `测试结果/20260921-120938/验证结果.txt`。发布包的“验证记录”提供该日志和默认面板渲染。

## 未宣称验证通过

未启动、读取或操纵 CS2，也未查询游戏进程或配置。未使用真实鼠标操作下方其他程序来验证穿透；自动检查的是窗口样式与状态。

未实测游戏内可见性、独占全屏、多显示器切换、实际 DPI 切换或游戏帧率。需要用户先在普通桌面、再在本地练习中手动检查。无边框／窗口模式是主要适配方向，普通窗口为备用方式。

没有 Valve/VAC 或其他平台的官方认证，也没有“零封禁风险”结论。
