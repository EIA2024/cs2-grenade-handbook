# 项目边界

这是独立的图片资料管理与桌面查看软件，CS2 仅为资料主题。

- 不查询游戏进程、窗口、安装位置、配置、日志、内存或网络，不使用 GSI。
- 不注入 DLL、安装驱动、使用游戏渲染或输入钩子，不模拟输入，不改游戏或反作弊设置。
- 系统窗口 API 只能接收本软件创建的窗口；不能查找或控制其他窗口，包括焦点恢复。
- 使用模式允许 Raw Input 后台接收键盘及鼠标按钮，只匹配绑定动作，不使用捕获、拦截或模拟输入；不处理鼠标位移，不保存输入日志。返回编辑器或退出时注销。
- HwndSource 的本窗口消息处理不是全局输入钩子。不为同一操作同时注册 RegisterHotKey；不安装设备驱动或读取任何游戏状态。
- 不因为显示兼容性问题改用侵入式方案。Game Bar 暂不实现。
- 本地测试仅使用本项目测试资料和本软件窗口；游戏兼容性由用户手动验证，不启动或读取游戏。
- 资料层与展示层分离，不在核心接口中加入游戏标识、进程或窗口参数。
- 构建：`pwsh -File 构建.ps1`；发布：`pwsh -File 构建.ps1 -Publish`。
- 验证：`pwsh -File 验证.ps1`；发布包验证加 `-Published`。
- 不把窗口属性测试或运行成功表述为真实鼠标穿透测试、游戏兼容通过或反作弊官方认证。

# 开发入口
- Core 为纯模型、导航、输入语义和设置规则；文件系统与 ZIP 实现在 Handbook.Storage；WPF 及原生 API 位于 Handbook.Desktop。
- Desktop 按 App、Editor、Presentation、Windows、Diagnostics 分目录。原生入口集中 Windows，核心不依赖 WPF 或存储实现。
- 版本来自 Desktop csproj；发布/验证/打包使用 scripts/发布配置.ps1 的同一份清单，打包不能收集清单之外的程序或用户文件。
- 详细安全约束与已知限制见 docs/05-安全审查与修复.md，目录和扩展入口见 docs/06-开发者架构.md。
